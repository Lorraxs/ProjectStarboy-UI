import React from "react";
import { useSelector } from "react-redux";
import { RootState } from "../store";

const Hud = () => {
  
  return <div>Hud</div>;
}

export default {
  element: Hud,
  needLogin: true
};
